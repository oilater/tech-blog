import { style } from "@vanilla-extract/css";

export const wrapper = style({
  margin: '0 auto',
  maxWidth: '768px',
  width: '100%',
});

export const feedContainer = style({
  marginBottom: "20px",
  fontSize: "32px",
});